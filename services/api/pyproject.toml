[project]
name = "datara-api"
version = "1.0.0"
description = "FastAPI gateway for Datara GPU services"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "torch",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
