name: CI - Image Tagging

on:
  push:
    paths:
      - 'services/image-tagging/**'
      - 'core/**'
      - '.github/workflows/tagging-ci.yml'
  pull_request:
    paths:
      - 'services/image-tagging/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/image-tagging/docker/Dockerfile
          push: false
          tags: datara-tagging:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
