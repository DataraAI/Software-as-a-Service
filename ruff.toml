# Ruff configuration
line-length = 120

[lint]
# Allow imports not at top of file (E402) for path hacks and stubs
# Ignore line length (E501) and import sorting (I001) for now to fix CI quickly
ignore = ["E402", "E501", "I001"]

# Enable common rules
select = ["E", "F", "W", "I"]
